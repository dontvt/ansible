# ansible
- Chuan bi truoc cac file vhost de copy len server(luu y: thay doi thong tin file trong install_nginx.yml cho phu hop)
- Chay file: sudo ansible-playbook -vv --ask-pass play_book_install_nginx.yml de install nginx tren cac server dc cau hinh trong host cua Ansible.
- play_book_install_nginx.yml doc cac file trong thu muc tasks de thuc hien install. 
